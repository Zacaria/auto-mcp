1. [ ] Initialize a Vite `react-ts` project under `web/` (or similar) and register it with the repo's pnpm workspace.
2. [ ] Install tailwindcss + postcss + autoprefixer, run `shadcn-ui init`, and import the base styles in `src/index.css`.
3. [ ] Add baseline dependencies: `ai`, `@modelcontextprotocol/sdk`, `openapi-types`, `react-router-dom`, and testing tools (Vitest + Testing Library).
4. [ ] Replace the default Vite counter with a placeholder layout that names the upcoming OpenAPI MCP builder; ensure `pnpm dev` renders it without errors.
5. [ ] Document dev/test scripts and environment expectations in `web/README.md`.
