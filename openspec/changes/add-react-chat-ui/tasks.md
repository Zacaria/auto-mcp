1. [ ] Add React Router with routes `/` (chat) and `/server` (status) plus navigation.
2. [ ] Implement `SpecInput` form (URL validation, submit states) and integrate with `/api/spec` using fetch hooks.
3. [ ] Embed the Vercel AI chat components in the root page, connecting `useChat` to `/api/chat` and rendering messages/tool calls.
4. [ ] Build `/server` page components (status header, tool list cards, cache metadata, restart/stop buttons) consuming `/api/server`.
5. [ ] Add component-level tests covering form validation, chat rendering stub, and status page layout (using mocked fetch responses).
